#Add dot at end of choices
class Window_ChoiceList < Window_Command
  def draw_item(index)
    @choice_enabled = command_enabled?(index) if defined?(WdTk::ChoiceEX)
    rect = item_rect_for_text(index)
    draw_text_ex(rect.x, rect.y, command_name(index)+".")
  end
end