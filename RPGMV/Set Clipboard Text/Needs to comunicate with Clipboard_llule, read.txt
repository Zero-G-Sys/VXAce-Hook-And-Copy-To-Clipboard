So it doesn't copy the new text again a modification in Clipboard_llule must be made (use the script in this folder if no modification to regex has been made)

replace this line

if (MemText != '') {

with

if (MemText != '' && ZERO.SetClipboardText.escapeText) {

-------------
Important
-------------

SetClipboardText must be loaded before Clipboard_llule